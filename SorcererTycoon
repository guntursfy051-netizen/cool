--// Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

--// Settings
_G.dis = CFrame.new(0,0,5)   -- Offset from boss
local skillCooldown = 1       -- Seconds between skill casts

--// Character handling
local hrp
local function updateCharacter(char)
    hrp = char:WaitForChild("HumanoidRootPart")
end
updateCharacter(player.Character or player.CharacterAdded:Wait())
player.CharacterAdded:Connect(updateCharacter)

--// Anti-ragdoll
local function antiRagdoll()
    local char = player.Character
    if not char then return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid.PlatformStand = false
        for _, v in pairs(char:GetChildren()) do
            if v:IsA("BodyPosition") or v:IsA("BodyGyro") or v:IsA("BodyVelocity") then
                v:Destroy()
            end
        end
    end
end

--// Skills Remote
local SkillsRemote = ReplicatedStorage:WaitForChild("Assets")
    :WaitForChild("Remotes"):WaitForChild("Skills"):WaitForChild("SKill")

--// Use skills
local function useSkills()
    local skillList = {
        {"Training Strike","Training Strike","InnateTechnique"},
        {"Anti-Magic Greatsword","Anti-Magic Cleave","Sword"},
        {"Toji","Brutal Strike"}, 
        {"Toji","Groundbreaker Punch"}, 
        {"Toji","Savage Kick Barrage"}, 
        {"Toji","Forward Devastation"}
    }

    for _, args in ipairs(skillList) do
        SkillsRemote:FireServer(unpack(args))
        task.wait(0.3)
    end
end

--// Teleport & face boss
local function handleBosses(bossFolder)
    for _, boss in pairs(bossFolder:GetChildren()) do
        local bossHRP = boss:FindFirstChild("HumanoidRootPart")
        local bossHumanoid = boss:FindFirstChildOfClass("Humanoid")
        if bossHRP and bossHumanoid and bossHumanoid.Health > 0 then
            hrp.CFrame = bossHRP.CFrame * _G.dis
            local direction = (bossHRP.Position - hrp.Position).Unit
            hrp.CFrame = CFrame.new(hrp.Position, hrp.Position + direction)
            return true
        end
    end
    return false
end

--// Toggle states for each button
local autoMetro = false
local autoLac = false
local autoWorld = false

--// Auto Metro
local function autoMetroBoss()
    autoMetro = true
    local lastSkillTime = 0
    while autoMetro do
        task.wait(0.1)
        antiRagdoll()
        local bossesFolder = workspace:WaitForChild("Map")
            :WaitForChild("Boss"):WaitForChild("Metro"):WaitForChild("Bosses")

        if handleBosses(bossesFolder) then
            if tick() - lastSkillTime >= skillCooldown then
                useSkills()
                lastSkillTime = tick()
            end
        else
            task.wait(1)
        end
    end
end

--// Auto Lac
local function autoLacBoss()
    autoLac = true
    local lastSkillTime = 0
    while autoLac do
        task.wait(0.1)
        antiRagdoll()
        local bossesFolder = workspace:WaitForChild("Map")
            :WaitForChild("Boss"):WaitForChild("Lac"):WaitForChild("Bosses")

        if handleBosses(bossesFolder) then
            if tick() - lastSkillTime >= skillCooldown then
                useSkills()
                lastSkillTime = tick()
            end
        else
            task.wait(1)
        end
    end
end

--// Auto WorldBoss
local function autoWorldBoss()
    autoWorld = true
    local lastSkillTime = 0
    while autoWorld do
        task.wait(0.1)
        antiRagdoll()
        local bossesFolder = workspace:WaitForChild("Map")
            :WaitForChild("Boss"):WaitForChild("WorldBoss"):WaitForChild("Bosses")

        if handleBosses(bossesFolder) then
            if tick() - lastSkillTime >= skillCooldown then
                useSkills()
                lastSkillTime = tick()
            end
        else
            task.wait(1)
        end
    end
end

--// GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "BossAutoGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 250)
mainFrame.Position = UDim2.new(0,50,0,50)
mainFrame.BackgroundColor3 = Color3.fromRGB(30,30,30)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui
mainFrame.Active = true
mainFrame.Draggable = true

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0,30)
title.Position = UDim2.new(0,0,0,0)
title.BackgroundColor3 = Color3.fromRGB(50,50,50)
title.Text = "Boss Auto Farm"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 18
title.Parent = mainFrame

--// Metro Button
local metroButton = Instance.new("TextButton")
metroButton.Size = UDim2.new(0,150,0,50)
metroButton.Position = UDim2.new(0,25,0,50)
metroButton.Text = "Auto Metro (OFF)"
metroButton.BackgroundColor3 = Color3.fromRGB(0,170,255)
metroButton.TextColor3 = Color3.fromRGB(255,255,255)
metroButton.Parent = mainFrame

metroButton.MouseButton1Click:Connect(function()
    if autoMetro then
        autoMetro = false
        metroButton.Text = "Auto Metro (OFF)"
        metroButton.BackgroundColor3 = Color3.fromRGB(0,170,255)
    else
        metroButton.Text = "Auto Metro (ON)"
        metroButton.BackgroundColor3 = Color3.fromRGB(0,255,0)
        spawn(autoMetroBoss)
    end
end)

--// Lac Button
local lacButton = Instance.new("TextButton")
lacButton.Size = UDim2.new(0,150,0,50)
lacButton.Position = UDim2.new(0,25,0,110)
lacButton.Text = "Auto Lac (OFF)"
lacButton.BackgroundColor3 = Color3.fromRGB(0,255,128)
lacButton.TextColor3 = Color3.fromRGB(0,0,0)
lacButton.Parent = mainFrame

lacButton.MouseButton1Click:Connect(function()
    if autoLac then
        autoLac = false
        lacButton.Text = "Auto Lac (OFF)"
        lacButton.BackgroundColor3 = Color3.fromRGB(0,255,128)
    else
        lacButton.Text = "Auto Lac (ON)"
        lacButton.BackgroundColor3 = Color3.fromRGB(0,255,0)
        spawn(autoLacBoss)
    end
end)

--// WorldBoss Button
local worldButton = Instance.new("TextButton")
worldButton.Size = UDim2.new(0,150,0,50)
worldButton.Position = UDim2.new(0,25,0,170)
worldButton.Text = "Auto WorldBoss (OFF)"
worldButton.BackgroundColor3 = Color3.fromRGB(255,170,0)
worldButton.TextColor3 = Color3.fromRGB(0,0,0)
worldButton.Parent = mainFrame

worldButton.MouseButton1Click:Connect(function()
    if autoWorld then
        autoWorld = false
        worldButton.Text = "Auto WorldBoss (OFF)"
        worldButton.BackgroundColor3 = Color3.fromRGB(255,170,0)
    else
        worldButton.Text = "Auto WorldBoss (ON)"
        worldButton.BackgroundColor3 = Color3.fromRGB(0,255,0)
        spawn(autoWorldBoss)
    end
end)

--// STOP Button
local stopButton = Instance.new("TextButton")
stopButton.Size = UDim2.new(0,150,0,50)
stopButton.Position = UDim2.new(0,25,0,230)
stopButton.Text = "STOP All"
stopButton.BackgroundColor3 = Color3.fromRGB(255,50,50)
stopButton.TextColor3 = Color3.fromRGB(255,255,255)
stopButton.Parent = mainFrame

stopButton.MouseButton1Click:Connect(function()
    autoMetro = false
    autoLac = false
    autoWorld = false

    metroButton.Text = "Auto Metro (OFF)"
    metroButton.BackgroundColor3 = Color3.fromRGB(0,170,255)
    lacButton.Text = "Auto Lac (OFF)"
    lacButton.BackgroundColor3 = Color3.fromRGB(0,255,128)
    worldButton.Text = "Auto WorldBoss (OFF)"
    worldButton.BackgroundColor3 = Color3.fromRGB(255,170,0)

    warn("All auto farms stopped manually.")
end)
